# HANDOFF — 세션 인수인계

> 마지막 갱신: 2026-09-25 · **이 커밋 기준** · origin/main 동기화 · 작업 규약은 [CLAUDE.md](CLAUDE.md) 참조
> (해시를 적지 않는다 — 문서가 제 커밋보다 항상 한 칸 뒤처져 낡아 보였다. 시점은 `git log -1 -- HANDOFF.md` 로 본다.)

## 현재 상태
- 배포: main 푸시 → GitHub Actions 가 레포 루트를 그대로 업로드. 공개 주소 <https://corporate-partners.kr>.
  ⚠️ 워크플로가 `concurrency: pages` 라 배포가 줄을 선다. 앞 실행이 `waiting` 으로 오래 묶이면
  큐 전체가 막히므로 `gh run list` 로 확인하고 멈춘 것을 취소해야 최신이 올라간다(2026-09-16 실제 발생).
- 미커밋 변경은 `guidelines/` 3개 파일 **삭제**뿐이다(워킹트리에 `D` 로 남아 있다). 미추적 파일(`mockups/`·`백엔드 참고문서.docx`)과 마찬가지로 **의도적 로컬 상태** — 커밋·복원하지 말 것.
- `docs/*.docx` 는 생성물이라 커밋하지 않는다. 원본은 `docs/backend-spec.md`.
- 개발 서버는 꺼져 있을 가능성 높음 → `node serve.mjs` (localhost:8000).
- 인계 문서 4종(README·HANDOFF·CLAUDE.md·`docs/backend-spec.md`)은 **2026-09-17 전수 감사**로 코드와
  대조했고(아래 '미결 감사와 정리'), 이후 커밋은 바꾼 코드의 문서 문장을 같은 커밋에서 고쳤다.
- **미결은 맨 아래 '보류 / 열린 항목' 한 곳에 모아 둔다**(2026-09-25 정리) — 코드로 끝낼 수 있는 미결은 없고,
  남은 것은 외부 입력 대기 · 결정된 후속 · 서버 전환 시 구현 항목이다. 화면·공용 모듈을 고쳤으면 같은 커밋에서 해당 문서 문장도 고칠 것 —
  낡은 규약은 다음 세션이 그대로 믿는다. ⚠️ 이 줄을 '대조 완료' 인 채로 두고 코드만 고치지 말 것:
  실제로 그렇게 두 커밋을 지나 네 문서 모두에 거짓 서술이 남아 있었다.

## 이번 구간(2026-09-25, 최신) — 알림톡 10종 초안 · 발급일 선택지 제한 · 법무 질문서

HANDOFF A 의 외부 대기 항목 중 **우리 쪽에서 먼저 써 둘 수 있는 것**(알림톡 문구·법무 질문서)을 채웠다. 그러다 드러난
문제 둘은 사용자 결정으로 닫았다.

- **알림톡 템플릿 10종**(명세 9.4 · T1~T10) — 기존 2종(마감 임박·자동 동의)에 7개 이벤트 8종을 더했다(가입은 승인·반려
  둘). 표에 **템플릿 코드 · 보내는 시점 · 수신자 · 버튼**을 적었다. 결정 두 개:
  - **주문 접수 확인(T1)은 관리자가 '주문접수'로 수락할 때** 보낸다(사용자 결정). 리본 문구를 싣는다 — 전화·카톡 주문을
    관리자가 옮겨 적으니 배송 전에 거래처가 볼 곳이 이것뿐이다.
  - **자동 동의 달도 두 통**(T5 + T6, 사용자 결정). 그래서 채택본 T5 의 "계산서가 발급되었습니다" 를 "이어서 발급되며,
    발급되면 승인번호와 함께 다시 안내" 로 고쳤다 — T5 시점엔 바로빌 발급 전일 수 있다(제목도 T6 과 겹치지 않게).
  - 작성 규칙: 변수 뒤 조사 금지(상품명 끝이 제각각), 빈 변수는 `-`, 링크는 버튼, **배송완료 리포트는 토큰 URL**
    (데모 `delivery/` 는 쿼리스트링에 이름·주소를 싣는다 — 아래 C).
- **발급일이 10·28일이면 동의 기간이 3시간뿐이었다** — 발급 10:00 과 마감 13:00 이 같은 날이다(9·27일 27시간,
  8·26일 51시간). 약관 ③항의 '마감 전날 고지'도 불가능했다. → **선택지를 1~7·11~25일로 줄였다**(사용자 결정 ·
  최소 75시간). `INVOICE_DAYS` 를 `consentWindowHours ≥ 72h` 에서 **파생**하고, 계산 함수는 1~28 을 그대로 받는다
  (제한 전 값·이력이 조용히 1일로 바뀌면 이미 발급된 달과 어긋난다). 예전 값은 드롭다운에 '지금은 고를 수 없는 날' 로
  보인다. 그룹(1~10 → 10일 · 11~28 → 28일)과 약관 문구는 그대로다.
- **법무 질문서 4문항**(명세 3.4, 세무 질문서 9.1 과 같은 형식) — ① 기한·고지의 충분성(최소 75h, 주말 마감 예:
  2026-10-10 토요일·전날 한글날) ② 수신자 없는 18곳 ③ **약관에 동의하기 전의 자동 동의**(새로 찾은 쟁점 — 아래)
  ④ 약관 ⑥항 "계산서 취소·수정 불가" 가 명세 9.1 의 수정계산서 규정·세법과 충돌. 세무 질문서에 4번(발급기한이
  주말이면 기한 특례가 적용되는가)을 더했다.

### 새로 드러난 것 (결정 대기 — 아래 '보류 / 열린 항목' A)
- **약관에 동의한 적 없는 거래처에도 자동 동의가 적용된다.** `store.agreementOf` 는 약관 동의 여부를 보지 않는다 —
  한 번도 로그인하지 않은 이관 거래처(약관 동의 기록 없음)도 마감이 지나면 자동 동의로 계산된다. 법무 질문서 3번.
- **'신규 가입 1회 무료'는 가입 화면에만 있다** — `register.js` 가 혜택으로 약속하는데 정산 규칙·명세 어디에도 없다
  (어느 주문이 0원인지, 명세서에 어떻게 찍히는지). T9 가 이 문장을 싣고 있어 카카오 검수 전에 정해야 한다.

### 검증
`test-settlement-rules` 127(+13: 동의 기간 7종 · 선택지 · 범위 문구 · 제한 전 값 계산) · `test-order-text` 39 ·
Playwright 9항목(선택지 22개 · 안내문 범위 · 25일 → 마감 28일 · 제한 전 9일 표시·계산 유지 · 7일로 변경 · 정산 표
렌더·NaN 0) · 콘솔 오류 0 · `build-docs.py` 정상.

## 이번 구간(2026-09-25) — 감사 이후 코드 점검: 로그인 결함 · 상태별 로그인 차단 · 중복 등록

2026-09-17 감사 뒤에 들어온 코드(약관 게이트·라우트 가드·프로필 거래처별)를 다시 읽고 실측했다. 결정 3건은 사용자에게 받았다.

- **로그인이 아무 입력이나 통과시켰다.** `resolveRole` 이 admin/0324 가 아니면 전부 enterprise 였고, 맞는 거래처가 없으면
  `util/client.js` 폴백으로 첫 거래처(태원과학) 포털에 들어갔다. 로그인 화면이 "아이디 또는 사업자번호"를 권해
  **뉴트리 사업자번호를 넣으면 태원과학 명세서**가 보였다(실측). → **아이디로만**(사용자 결정 · 명세 6.2) 받고, 모르는 아이디는
  `AUTH_FAILED` 문구로 거절한다. 관리자 비밀번호를 틀려도 태원과학으로 들어가던 것이 같이 막혔다.
- **약관 게이트 우회** — 역할을 게이트 **전에** 줘서, 게이트가 떠 있는 동안 주소창에 `#/app/invoice` 를 치면 역할이 풀린
  채로 명세서 화면이 떴다. 역할은 동의한 뒤에만 준다(`login.js::enterPortal`).
- **승인대기·반려·정지는 로그인 자체를 막는다**(사용자 결정 — 명세는 '주문만 불가, 서버 필요'였다). 로그인 화면이 상태별
  문구(반려는 사유까지)를 띄우고, 이미 열린 세션은 라우터가 화면을 옮길 때마다 다시 보고 끊는다 — 로그인 뒤 관리자가
  정지했거나 **지운** 거래처도 여기서 끊긴다(지운 경우는 폴백으로 태원과학이 보였다). 판정·문구는 `util/client.js::portalBlock` 하나.
- **접속 아이디 중복을 막는다**(명세 5.2 UNIQUE) — 가입 1단계와 관리자 신규 등록이 `store.accountIdTaken` 을 같이 쓴다.
  예전엔 이관 계정과 같은 아이디로 가입할 수 있었고, 그 계정은 영영 로그인할 수 없었다. `admin` 은 예약어.
- **관리자 거래처 '등록'을 두 번 누르면 같은 id(C020)의 레코드가 둘 생겼다** — `isEdit` 가 상수라 등록 뒤에도 버튼이
  '등록'이었다. 등록하는 순간 그 거래처의 수정 창으로 바뀐다(헤더 상태 pill · 아이디 읽기전용 · 변경 이력).
- **실거래처 개인정보는 그대로 둔다**(사용자 결정) — 저장소가 public 이고 사이트도 공개인데 `admin-mock.js` 거래처 시드에
  실제 대표자명 19·휴대폰 15·거래 조건 속 인명, `staff-mock.js` 에 실번호로 보이는 2개가 있다. `session.js` 의
  "실제 개인정보 없음" 주석만 사실대로 고쳤다(근거는 아래 E).

### 검증
Playwright 34항목 — 문구 · 모르는 아이디/사업자번호/관리자 오답 거절 · 게이트 중 주소 이동 차단 · 동의 후 딥링크 복귀 ·
정지 후 새로고침 → 세션 끊김 + 안내 1회 · 승인대기/반려(사유) 차단 · 지운 거래처 세션 끊김 · 가입 중복·예약 아이디 거절 ·
가입 → 승인대기 차단 → 승인 → 로그인 · 관리자 중복 아이디 잠금 · 등록 후 수정 모드 · 두 번 눌러도 1건. 전 화면 스모크
(관리자 13화면 · 거래처 6화면×2) 콘솔 오류 0 · `test-settlement-rules` 114 · `test-order-text` 39.

## 이번 구간(2026-09-17) — 보류 4건 정리: 라우트 가드 · 도넛 동적 · 링크 만료·주문 연동은 서버 몫

- **`#/app/*` 라우트 가드** — 역할이 없으면 `#/login` 으로 보내고, 로그인하면 들어오려던 주소로 돌아온다
  (`session.setReturnTo/takeReturnTo`, 관리자 딥링크도 같은 길). 예전엔 주소만 치면 태원과학 명세서 60건이 보였다.
- **대쉬보드 도넛** — 9종 고정 순회를 실제 품목 키 순회로(리포트와 같은 방식). '특대 꽃바구니' 가 들어오고 항목 합계 = 총계.
- **공개 링크 만료·열람 로그** — 명세 8.3 을 서버 구현 항목으로 확정, 데모 무변경. 열람 로그는 화면 없이 DB 만.
- **포털↔관리자 주문 연동** — 서버 붙일 때(재확인). 포털 퍼널의 '주문 접수하기'는 지금 어디에도 저장하지 않는다.

## 이번 구간(2026-09-17) — Q4 바로빌: 면세 API 방식 · 인증서 · 세무 질문서

외부 자료(바로빌 개발자센터·팝빌 인증서 안내·국세청 발급시기)로 확인한 뒤 결정을 받았다. 코드 변경 없음, 명세 9.1·10.4.
- **면세 계산서 = 전자세금계산서 API 에 `TaxType=면세`, 세액 0.** 별도 메서드가 아니다(발급 전 과세유형 검증). 메서드명은 착수 시 레퍼런스 확인.
- **인증서는 대행사(바로빌) 보관** — 운영이 바로빌 웹에서 직접 등록. 인증서 파일·비밀번호는 우리 시스템을 지나지 않는다.
- **11~28 그룹 작성일자(동의일)는 규칙 유지 + 세무대리인 질문서 3문항 첨부**(명세 9.1). 답이 오면 규칙을 다시 본다 —
  대안은 작성일자 = 귀속월 말일(지연발급 가산세 리스크) 또는 발급일 1~10일만 허용.

## 이번 구간(2026-09-17) — Q6 잔여: 이용약관 동의 게이트 · 알림톡 템플릿

규칙(마감 10일/28일 13:00 · 자동 동의 · 작성일자)은 확정돼 있었고 문구와 사전 고지 경로만 남아 있었다.
조사해 보니 **회원가입에 약관 동의 단계가 아예 없었고**, 안내를 받을 정산·회계 담당자가 있는 거래처는 19곳 중
1곳뿐이었다(대표 연락처 16곳 · 계산서 이메일 0곳). 결정과 구현:

- **가입 3단계에 이용약관 동의 체크 신설**(공용 `.dlg-check`, '약관 보기' 링크) — 체크 없이는 가입이 끝나지 않는다.
  거래처 레코드에 `termsAgreedAt`·`termsVersion` 을 남긴다.
- **이관 거래처는 첫 로그인 게이트** — `login.js` 가 `termsVersion !== TERMS_VERSION` 이면 포털로 보내기 전에
  게이트 다이얼로그(`util/terms-dialog.js`, gate 모드)를 띄운다. 닫으면 로그인 실패 취급(역할·거래처 해제 + 오류 문구).
  조항 뜻이 바뀌면 `TERMS_VERSION` 을 올려 재동의를 받는다.
- **조항·알림톡 템플릿 초안 채택**(법무·카카오 검수 전제) — 본문 단일 소스 `js/data/terms.js`, 템플릿 2종은 명세 9.4.
  "기한 내 무응답 = 동의" 의제는 상당한 기한 + 별도 고지가 있어야 유효로 본다(약관규제법 12조, 검토 필요) —
  그래서 마감 전날 알림톡은 조항의 요건이다.
- **발송 시점**: 마감 전날 10:00 한 번(발급일 10일·28일 거래처는 발급 직후).
- **수신자 없는 거래처(정산담당 0명)에는 보내지 않는다** — 대표 연락처 폴백 없음(사용자 결정). 대신 관리자 정산 표의
  거래처 칸에 '알림 수신자 없음' 경고(`.settle-td__sub--warn`). ⚠️ 자동 동의는 그대로 진행되므로 사전 고지 없는
  의제가 되어 **법무 검토가 필요하다** — HANDOFF 열린 항목에 남긴다.
- ⚠️ **Playwright 로그인 스크립트는 게이트를 지나야 한다** — 거래처 계정으로 처음 로그인하면 다이얼로그가 뜬다
  (`[data-action='ack']` 체크 → `[data-action='ok']`). 관리자(admin)는 거래처가 아니라 게이트가 없다.

## 이번 구간(2026-09-17) — 명세 10.4 미결 13건을 사용자와 하나씩 결정

각 항목마다 "코드가 지금 실제로 하는 일 → 무슨 일이 생기나 → 선택지" 를 실측으로 보여 주고 결정을 받았다.
결정은 전부 명세(3.4 · 4.6 · 4.7 · 5.3 · 5.9 · 6.6 · 8.4 · 9.4 · 10.4)에 확정으로 적었고, 코드가 바뀐 것은 아래에 표시.

| # | 결정 | 코드 |
|---|---|---|
| Q1 부분 입금 | **불허** — 전액 입금까지 미입금, 이월 없음 | — |
| Q2 단가 이력 | **이력 없음·소급 없음** — 주문 amount 는 접수 시점 스냅샷(이미 그렇다). 감사 로그 대상에서도 '단가' 제외 | — |
| Q3 감사 로그 | **조회 화면 없음·기록만.** 거래처 모달 '변경 이력' 카드는 서버 감사 로그로 채운다 | 주석 1줄 |
| Q7 신용 한도 | **자동 차단 없음.** 기한 초과 경고 + 알림톡(9.4 에 이벤트 추가), 반복 시 관리자가 '정지' | — |
| Q10 배송모드 | **저장하지 않는다** — 당일/예약은 배송일 vs 오늘 파생 유지 | — |
| Q11 채널 수수료 | **개념 자체가 없다**(디자인 단계 오류) — `B2C_CHANNEL_META.fee`·등록 모달 표기 4곳·CSS·명세에서 제거. `pay_status` 는 사실로만 | ✔ |
| Q12 주문일시 키 | **서버는 `ordered_at` 하나**(ISO 8601 +09:00). 프런트 두 키는 API 어댑터에서 매핑 | — |
| Q13 프로필 귀속 | **거래처별** — `store.profilesByClient` + `profilesOf(clientId)`, 구 저장본(전역 배열)은 첫 거래처로 이관. 포털·주문 퍼널·관리자 B2B 피커 3곳 | ✔ |
| Q14 B2C 알림 | **주문자에게만.** 명단·프로필 없음 | — |
| Q8 모바일 | **재정의** — 웹 반응형 아님. 추후 **모바일 앱 연동**, 상세는 그때 | — |
| Q4 · Q6 | 항목 3·4(약관·알림톡 문구, 바로빌)로 이어서 | |

⚠️ 발송 프로필이 거래처별이 되면서 **시드 5건은 태원과학(C001) 소속**이다. 다른 거래처로 로그인하면
프로필이 0건이 정상이다(직접 등록해야 한다). 관리자 B2B 등록 모달의 프로필 피커도 고른 거래처 것만 보여 준다.

### 검증
`node tools/test-settlement-rules.mjs` 114 · `test-order-text.mjs` 39 · Playwright: 구 저장본(전역 `profiles`)이
태원과학으로 이관 · 뉴트리 0건 → 등록 1건 → 태원과학 화면엔 안 보임 · 관리자 B2B 피커 뉴트리 1건/소담채 0건 ·
B2C 등록 모달 수수료 흔적 0, 푸터 "네이버 스토어 · 선결제 완료" · 콘솔 오류 0.

## 이번 구간(2026-09-17) — 미결 감사와 정리

"미결 작업이 있는지" 물음에 감으로 답하지 않으려고 레포 전체를 감사했다(문서 드리프트 · 코드 잔여물 ·
공식 미결 목록 세 축, 후보 33건 → 확정 22건 + 완결성 비평 4건, 기각 11건). 확정분을 두 커밋으로 닫았다.

### 고친 결함 (앞의 둘은 직전 커밋 6ac099cb 이 만든 회귀다)
- **발급일을 바꾸면 과거 6개월이 다시 계산됐다.** 발행일·마감·작성일자를 매번 *현재* invoiceDay 로
  파생했기 때문이다 — 수동 동의한 달의 작성일자가 09-17 → 08-31 로 밀리고, 이미 자동 동의로 계산서가
  나간 달이 '동의대기' 로 되돌아가 포털에 동의 버튼이 다시 떴다(실측). **발급일 변경은 다음 달
  발급분부터 적용**(사용자 결정)으로 바꿨다 — `settlement-rules.invoiceDayFor(client, period)` 가 단일
  소스이고 이력은 `client.invoiceDayLog` 에 `store.updateClient` 가 자동으로 쌓는다.
- **거래가 없는 달의 A4 가 '계산서 발행: 발급완료'** 로 찍혔다(합계 0원·발행일 '-' 옆에서). 빈 달은 '-' 다.
- **월간 리포트의 항목별 합계가 총계와 달랐다** — 9종 고정 순회라 '특대 꽃바구니' 가 빠졌다
  (2026-08 실측 33,360,000 vs 33,210,000). 실제 집계 키를 훑도록 고쳐 세 달 모두 일치시켰다.
- **공개 열람 링크가 화면·PDF 와 다른 금액을 열었다.** 시드 토큰 3개가 품목·합계를 손으로 들고 있었고,
  `issueLink` 가 같은 (거래처, 귀속월)이면 호출부가 만든 doc 을 **버리고** 시드를 돌려줬다(C001 04월
  410,000 vs 180,000). 시드 doc 을 INVOICE_DB 에서 파생하고, 토큰은 재사용하되 **본문은 늘 최신으로 덮는다.**
- **발행 전(발급예정) 달도 관리자가 링크·PDF 를 내보낼 수 있었다** — 금액이 아직 쌓이는 중인 달이다.
  두 버튼을 잠그고 이유를 title 로 말한다(포털은 이미 네 버튼을 잠근다). 명세 6.6 에도 `409` 를 적었다.
- **포털 정산표의 'N월 명세서 조회' 가 그 달을 열지 않았다**(늘 최근 달). 라우터가 해시 완전일치라
  쿼리를 못 실어 `util/settlement.js` 의 세션 키로 넘긴다 — 받는 쪽이 읽고 바로 지운다.
- **토스트가 스크린리더에 전혀 읽히지 않았다.** 지속형 live-region 을 따로 두고(보이는 노드는
  `aria-hidden`) 경고는 `assertive`, 성공은 `polite` 로 읽는다.

### 위생 정리 (병렬 스윕 + 독립 검증)
- 미사용 import 13개(6개 파일) · 죽은 export 5개(`B2C_STAFF`·`RIBBON_FLAT`·`dom.clear`·`isAuthed`·`aoaToXlsx`) ·
  고아 CSS 6블록(`.cli-attach*`·`.hm-preview*`·`.hm-radio*`·`.section-card*`·`.hm-findrow`·`.shell__logo-text`).
  ⚠️ `order-screen.js` 의 `pad2`·`dash`·`dtpMarkup`·`card`·`autosize` 는 **본문 미사용이지만 재수출 대상**이라
  남겼다(지우면 두 화면이 죽는다). `.modal-panel--sm` 은 `modal-panel--${size}` 동적 조합이라 살아 있다.
- **판단해서 남긴 것**: `.btn-primary`·`.btn-success`·`.tint-green`·`.tint-orange`·`.field-error`·`.hm-success` 는
  사용처 0 이지만 **공용 키트의 변형**이다(형제가 살아 있다). 색 세트를 반쪽만 남기는 쪽이 더 나쁘다고 보고
  유지했다 — 다음 감사에서 다시 올라오면 이 문장을 근거로 넘길 것.
- `'YYYY년 MM월'` **조인 키** 포맷이 독립 구현 다섯 벌이었다 → `settlement-rules.periodLabel` 하나로 모았다.
  `admin-orders.js` 의 동명이인 로컬 함수는 `orderPeriodLabel` 로 갈랐다(섀도잉 방지).

### 문서 드리프트
CLAUDE.md(동의 기록이 관리자와 '별개' 라던 낡은 문장 · `b2c/b2bSetStatus` 표기) · backend-spec(카탈로그
20 → 21종 · Profile 고정문구·수신번호 필수 · 링크 발급 409) · README(영속 키에 동의 기록 · 회귀 테스트 2종) ·
HANDOFF(미결 12 → 13건, Q14 누락).

### 검증
`node tools/test-settlement-rules.mjs` 114 · `node tools/test-order-text.mjs` 39 · 전 JS `node --check` ·
전 CSS 중괄호 균형 · Playwright(발급일 변경 전후 정산 행 불변 · 빈 달 문서 '-' · 발행 전 버튼 잠금 ·
드릴다운 합계 300,000 · 정산표→명세서 월 전달 · 대쉬보드 NaN 0 · 공개 링크 페이지) · 콘솔 오류 0.

## 이번 구간(2026-09-17) — 계산서 발급 동의 규칙 연결 · 전 품목 면세

포털의 동의 기록(`store.invoiceAgreed`)과 관리자 정산의 발급 상태(`발행일 ≤ 지금` 파생)가 **서로 다른
근거**로 같은 사실을 말하던 것을, 사용자가 확정한 실제 업무 규칙(질문 21개)으로 이었다.

- **규칙 단일 소스 `js/data/settlement-rules.js`**(import 0 · `node tools/test-settlement-rules.mjs` 93케이스).
  발행 = 귀속월 다음 달 `invoiceDay` **10:00** · 동의 마감 = 발급월 **10일 13:00**(발급일 1~10) /
  **28일 13:00**(11~28) · 마감 경과 = **자동 동의**(기록 없이 파생) · 작성일자 = 귀속월 말일(1~10) /
  동의한 날(11~28, 자동이면 28일) · 정산기한 = 발행월 말일. 기간은 `"YYYY-MM"` 만 받는다 —
  0-based 월 함정을 구조로 막았고 `admin-clients` 의 `monthEnd` 는 지웠다.
- **동의 상태 파생 `store.agreementOf`** 하나를 포털 버튼 · `agreeInvoice` 쓰기 가드(열려 있지 않으면 `null`) ·
  관리자 표가 같이 본다. 정산 행은 **`js/util/settlement.js::settlementsFor`** = admin-mock
  `settlementBaseRows`(날짜·금액) + 동의 오버레이. 옛 `"동의하기"` 센티널은 폐기 — 관리자 PDF 의
  '계산서 발행' 칸에 그대로 새던 결함이 같이 사라졌다(어휘는 `INVOICE_NOTE` 한 벌: 발급대기/발급완료).
- **포털 거래명세서 동의 카드가 네 상태**가 됐다(발행 전 · 열림 · 수동 · 자동). 캡션이 마감 시각과
  작성일자 규칙을 그룹별로 말하고, 다이얼로그 요약에 작성일자 행이 붙었다. 카드는 `aria-live` 슬롯이고
  동의 직후 포커스가 결과 문구로 간다.
- **관리자 정산 표는 배지 + 보조줄**(`동의완료 · 09-17 수동` / `09-10 자동` / `동의대기 · 마감 09-28 13:00` /
  `발급예정 · 10-01 10:00` · `발급완료 · 작성 08-31`). `.settle-badge*` 는 components.css 로 옮겼다(관리자·포털 공용).
- **전 품목 면세 → 문서는 '계산서'**. UI·명세서·회원가입·백엔드 명세의 '세금계산서'·'(VAT 별도)' 를 전부
  걷어냈다(`grep 세금계산서` 는 "…가 아니다" 부정 용례만 남는다). 명세 9.1 은 바로빌 **면세 계산서**로 다시
  썼고 메서드명은 미결로 표시했다. 5.4 의 `tax_invoice_*` 는 `einvoice_*` 로 개명.
- **실데이터**: (주)뉴트리(C011) 2026-08 4건·300,000원(사용자 전달)을 `invoice-mock.js` 에 **거래처별**로
  넣었다(기존 3·4·6월은 태원과학 소속). 상품명은 카탈로그명으로 옮기고 **'특대 꽃바구니'(생화 150,000원)를
  신설**했다(`tools/test-order-text.mjs` 의 CATALOG 에도). **실데이터가 있는 (거래처, 달)은 관리자
  청구금액·드릴다운·리포트도 그 행으로 계산한다**(`usageFor` 우선) — 뉴트리 8월 300,000원, 태원과학 6월 3,600,000원.
- 뉴트리 발급일을 **15일**로 바꿨다(11~28 그룹 시연). ⚠️ `hydrate()` 는 저장값이 시드를 이기므로
  **localStorage 가 남아 있는 브라우저에서는 1일로 보인다** — 데모 전 저장소를 비우거나 관리자에서 15일로 맞출 것.
  로그인은 `nutree` + 아무 비밀번호(빈칸은 폼이 막는다).

### 검증 (Playwright 헤드리스 · 매 시나리오 localStorage 비운 뒤 · 콘솔 오류 0)
- `nutree`: 기본 08월 · 4건 · 300,000원 · 발행일 2026년 09월 15일 · 캡션 "9월 28일 13:00 … 작성일자는 동의한 날" ·
  다이얼로그 요약 `작성일자 2026-09-17 (오늘)` + 힌트줄 · 체크 게이트 → 동의 → 카드 "동의 2026-09-17 15:14 ·
  작성일자 2026-09-17" · 문서 '계산서 발행' 발급대기→발급완료 · 토스트 "… · 작성일자 2026-09-17" · 포커스
  `.iv-agree__done` · **새로고침 후 유지** · 빈 달(07·09)은 네 버튼 잠김 · 정산 간편조회 안내 두 문단 +
  `발급완료 작성 09-17` / `발급대기 명세서 10-15 10:00 발급` · 상품안내에 특대 꽃바구니.
- `taewonsci`: 06·04·03월 자동 카드(07-10 13:00 · 06-30 …) · 버튼 없음 · 문서 발급완료 · 안내문 "10일 13:00 … 귀속월 말일".
- `admin`: 2026-09 뷰 전 행 `발급예정 10-01 10:00`(뉴트리 10-15) · 2026-08 뷰 뉴트리 `300,000원 · 동의완료 09-17 수동 ·
  발급완료 작성 09-17`(포털에서 동의한 직후, 저장소 유지), 나머지 `동의완료 09-10 자동 · 발급완료 작성 08-31` ·
  드릴다운 3단화환(기본형) 3건 150,000 + 특대 꽃바구니 1건 150,000 = 합계 300,000 · 06월 태원과학 3,600,000원(60건) ·
  정산 진행 19/19·19/19 · 공개 링크 레코드의 '계산서 발행' = 발급완료(뉴트리 08) / 발급대기(태원 09) ·
  대쉬보드 4단계 19·18·18·0(저장소 비운 뒤라 뉴트리 8월이 동의대기) · NaN 0 · 거래처 모달(뉴트리) 안내문
  "28일 13:00" + 헤더 "명세서 매월 15일 · 동의 마감 28일 13:00" → 발급일 1일로 바꾸면 즉시 "10일 13:00" ·
  공개 링크 `/invoice/?link=FP9S0QA8YA` 정상(발급대기). 회원가입 3단계의 이메일 placeholder 는 문자열 치환만(브라우저 미확인).
- `node tools/test-settlement-rules.mjs` 93/93 · `node tools/test-order-text.mjs` 39/39 · CSS 중괄호 균형 · `build-docs.py` 정상.
- 검증 후 localStorage 를 비워 되돌렸다.

## 이번 구간(2026-09-17 밤) — 거래명세서 조회 리모델링 시안 이식 · **시안 4종 완료**

`거래명세서 조회 - 리모델링.dc.html` 을 이식했다. 이걸로 그 디자인 프로젝트의 **시안 4개가 전부**
이식됐다(상품 규격 안내 · 전체 모달 · 프로필 저장공간 · 거래명세서).

- 우측이 **연·월 드롭다운 2개 + 월 스테퍼** → **12개월 목록**이 됐다. 달마다 거래 건수·금액이
  함께 보여 "6월 얼마였지"를 드롭다운 두 번 여닫지 않고 바로 본다. 좌측 A4 는 회색 면 위에
  그림자를 깔고 **미리보기만 자체 스크롤**한다.
- **`js/invoice-doc.js` 는 한 글자도 안 고쳤다** — A4 서식과 여러 장 분할이 이미 거기 있었고
  (시안 스크립트의 `paginate()` 와 같은 알고리즘), 공개 열람 링크·관리자 정산이 같은 모듈을 쓴다.
- 목데이터 200줄을 **`js/data/invoice-mock.js`** 로 분리했다. 기본 선택월은 '거래가 있는 가장
  최근 달'을 파생한다(상수 아님).
- **계산서 발급 동의가 영속된다** — `store.invoiceAgreed`, **거래처별 · 귀속월별 · 시각까지**.
  동의하면 문서의 '계산서 발행' 칸도 '발급완료' 로 바뀐다. 재확인은 `.dlg-check` 체크 게이트.
  ⚠️ 관리자 정산의 발급 상태 파생과는 별개였다 — **다음 구간(맨 위)에서 규칙으로 이었다.**
- PDF·EXCEL·열람링크는 **현행 실제 동작 그대로**다(시안은 토스트만 띄우는 목업이었다).

### 시안 캔버스(1600)가 가린 것 두 개 — 실측으로 잡아 보완
- **A4 가 좁은 화면에 안 들어간다.** 794px 고정폭인데 1366 창에선 좌측 칸이 654px 뿐이다.
  `transform: scale()` + **감싼 칸의 폭·높이를 JS 가 직접 지정**(transform 은 레이아웃 박스를
  줄이지 않는다) + 머리글 `맞춤`/`100%` 토글 + `ResizeObserver`. 1366 에서 0.802 배, 가로 넘침 0.
  인쇄는 `.invoice-doc` 의 outerHTML 만 새 창에 옮기므로 **배율의 영향을 받지 않는다**(구조로 확인).
- **768 높이에서 월 목록이 2개월만 보인다.** 아래 카드가 ~360px 를 쓴다. 뷰포트 높이 **860px
  미만이면 화면 고정을 포기하고 전체 스크롤**로 되돌린다(사용자가 고른 경계값).

### 검증 (Playwright · 1600×1000 / 1366×1000 / 1366×768 / 1600×900 / 1600×840)
기본 선택월 06월 + 목록 가운데 정렬 · 60건이 A4 4장 + 'A4 · 4장' pill · 월 전환 시 문서·금액·건수·
발행일 동시 갱신 + **목록 스크롤 유지** · 빈 해(2019)에서 12개월 전부 '내역 없음' + **네 버튼 모두
잠김** · 배율 토글과 리사이즈 재계산 · 열람링크 실제 토큰이 클립보드에 + 경고 토스트 ·
동의 체크 게이트 → 발급 → **새로고침 후에도 유지, 다른 달은 미동의** · 860 경계 전환 · 콘솔 오류 0.
회귀: 공개 링크 페이지(`/invoice/?link=…`)와 관리자 정산회계 화면 정상.
검증에 쓴 동의 기록·발급 토큰은 `localStorage` 를 비워 되돌렸다.

## 이번 구간(2026-09-17 저녁) — 프로필 저장공간 리모델링 시안 이식

같은 디자인 프로젝트의 **세 번째(마지막) 시안** `프로필 저장공간 - 리모델링.dc.html` 을 이식했다.
이 이식의 본체는 새 CSS 가 아니라 **공용 다이얼로그 셸 재사용**이다 — 시안 주석부터
"admin dialog.js 규격"이라고 적고 있다.
- 화면이 **섹션 카드 2장**(발송인 프로필 · 담당자)이 됐다. 각 카드 머리글에 제목 + 개수 +
  설명 + 등록 버튼이 모이고, 표는 무테(셀 세로줄 없음)다.
- 등록·수정·삭제 **다이얼로그 3종을 전부 공용 규격으로** 갈아끼웠다 —
  등록/수정 `openDialog`(560px) + `dlgRule`/`dlgRow`/`dlgActions`, 삭제 `openDeleteConfirm`
  (체크 게이트), 정산담당 삭제 차단 `openDialog`(520px). 예전의 손수 짠
  `.hm__head`/`.hm__foot` 모달 네 벌이 사라졌다.
- **리본 고정문구가 필수가 됐다**(사용자 결정). 비면 등록이 잠기고 **푸터가 무엇이 남았는지
  이름을 대서** 말한다. 예전의 '비우면 직위+성함 자동생성'은 제거했다.

### 공용 모듈 둘을 옮겼다 (사용자 승인)
- **`pageHead()` → `js/ui.js` + `.page-hd`(components.css)** — 리모델 시안 머리글(28px + 부제)이
  이제 두 화면에서 쓰인다. 상품 규격 안내도 손수 짠 머리글을 걷어내고 이걸 쓴다.
  옛 화면 15개는 `pageTitle`(22px) 그대로다 — **다른 규격이니 섞지 말 것.**
- **`openDeleteConfirm` → `js/util/dialog.js`** (order-screen 이 재수출). `openDialog` 위에 세운
  것이라 원래 거기가 집이고, `admin-clients.js`(주문 화면이 아니다)도 이미 order-screen 에서
  끌어다 쓰고 있었다. 호출부 4곳 무변경.

### 검증 (Playwright · 1600×1000 / 1366×768 / 1180×800)
필수 게이트(성함·연락처만 채우면 **힌트가 '고정문구'를 지목**하고 버튼이 잠김) · 타이핑 중
**포커스·커서 유지**(재렌더 없음) · 연락처 하이픈 자동 · 담당자 토글 `aria-checked` ·
**정산담당 지정이 저장 후에야 표에 반영** · 정산담당 1명 불변식 유지 · 정산담당 행은 삭제 잠김 ·
체크 게이트 삭제 · 토스트 · 반응형 3폭 가로 넘침 0 · 콘솔 오류 0.
**회귀 확인**: 포털에서 만든 담당자가 `#/admin` 담당자 관리 다이얼로그에 그대로 보임(같은 레코드) ·
거래처 삭제 다이얼로그 · 주문서 삭제 다이얼로그 · 상품 규격 안내 머리글(28px) 모두 정상.
검증에 쓴 목데이터는 `localStorage` 를 비워 시드로 되돌렸다(프로필 5 · 담당자 3 · 정산담당 오임찬).

## 이번 구간(2026-09-17) — 상품 규격 안내 리모델링 시안 이식

claude.ai/design 프로젝트 `76f0c42f-…cceaae` 의 **`상품 규격 안내 - 리모델링.dc.html`** 을 이식했다.
같은 프로젝트에 `현행 상품 규격 안내.dc.html` 이 함께 있어 둘을 대조했고, 현행 시안이 이식 전 구현과
1:1 이라 **두 시안의 차이가 그대로 작업 범위**였다.
- 화면이 **스티키 바 + [표 | 400px 고정 상세 패널] 2단**이 됐다. 행을 훑는 동안 사진·금액·규격이
  우측에서 바뀐다 — **행별 '샘플사진 보기' 모달과 캐러셀은 제거**(되살리지 말 것). 표 열도 바뀌었다:
  금액이 맨 끝으로 가고 샘플사진 열이 사라졌으며 즐겨찾기가 체크박스 → **별 토글**이 됐다.
- **하단 '지역별 반입가이드' 표(전국 52곳)는 시안대로 내렸다** — 사용자 지시는 "시안대로 제거하지만
  데이터는 남겨두세요"라 `js/data/intake-guide.js` 는 **디스크에 그대로 두고 참조만 끊었다**.
  지금은 어느 화면도 import 하지 않는 고아 모듈이고, 그 사유를 `products.js` 상단 주석에 박아 뒀다.
  실제 반입 제한은 주문 퍼널이 `intake-rules` 엔진으로 여전히 거른다.
- `onRowOpen` 을 `util/order-screen.js` → **`js/ui.js`**(`tableGrid` 옆)로 옮기고 order-screen 에서
  재수출했다. 상품 화면이 주문 모듈을 import 하면 드롭다운·피커·모달 그래프가 통째로 딸려 온다.
  기존 호출부 3곳은 손대지 않았고, `#/app/orders` 행 클릭이 그대로 도는 것을 확인했다.
- 토큰 2개 추가: `--c-orange-soft-hv`(선택 행 hover) · `--c-glass-bar`(스티키 바 유리면).
  `backdrop-filter` 는 **이 레포 첫 사용**이다 — 그 바 안에 팝오버를 넣으면 안 된다(주석으로 경고).

### 시안과 일부러 다르게 한 것 (셋)
- **카테고리 pill 은 `<button>` 이 아니라 진짜 라디오 그룹**이다(`.sr-only` clip + `:checked + span`).
  픽셀은 같고 탭 스톱이 1개 + 화살표 이동이 공짜다. 버튼 5개면 탭 스톱 5개에 화살표가 없다.
  ⚠️ 그래서 **필터 변경에 페이지를 다시 그리면 안 된다** — 실측으로 잡은 결함이다. 처음엔 `render()`
  전체를 돌렸는데 포커스를 쥔 라디오가 갈려 나가 **화살표를 한 번밖에 못 눌렀다**. 슬롯 3개
  (`updateTable`/`updateDetail`/`updateSave`)만 갱신하도록 바꿔 4연속 이동을 확인했다.
- **상세 패널 높이를 뷰포트에 묶었다.** 시안 캔버스가 1600×1000 이라 카드가 ≈850px 인데 1366×768
  에서는 606px 만 남아 **즐겨찾기 토글과 면책 문구가 영구히 화면 밖**이었다(스티키는 자기보다 큰
  영역을 끌어올 수 없다). 사진에 `max-height: min(38vh,420px)`, 카드에 `max-height`+자체 스크롤.
- **패널 사진 클릭 → 라이트박스는 남겼다**(시안엔 없지만 현행 기능이었다. hover 때만 뜨는 힌트라
  외형은 그대로). 불필요하면 이 조각만 빼면 된다.

### 검증 (Playwright · 1600×1000 / 1366×768 / 1180×800)
별↔패널 양방향 동기화(괄호 든 key `근조오브제(1단형)` 포함) · 별 클릭이 선택을 바꾸지 않음 ·
드래그 선택 중 행 열림 차단 · 카테고리 5종 전환과 선택 자동 이동 · 저장 3상태(2.2초) ·
**저장 직후 화면을 떠나도 타이머가 다음 페이지 DOM 을 치지 않음** · 스티키 고정 · 1366 에서 즐겨찾기
버튼 도달 가능 · ≤1240 에서 패널이 표 위로 · 가로 넘침 0 · 계약단가 경유 · 콘솔 오류 0.
`#/app/orders` 행 클릭 회귀 없음, `.msplit*` 규칙 그대로(`.pcar*` 는 아래 '죽은 CSS 정리'에서 걷어냈다).
⚠️ 검증 중 규약의 함정을 그대로 밟았다 — **해시가 같은 `goto` 는 ES 모듈을 재로드하지 않아**
한동안 옛 코드를 검증하고 있었다. `page.reload({waitUntil:"networkidle"})` 로 다시 확인했다.

### 죽은 CSS 정리 (같은 날 후속)
리모델로 사용처가 0 이 된 규칙을 걷어냈다. 지운 것은 **`.pcar*`**(샘플 사진 캐러셀 — 옛 products.js 가
유일한 사용처)와 **`.hm-help`**(옛 샘플 모달의 면책 문구 — 새 화면은 `.prod-card__fine` 을 쓴다) 둘이다.
- ⚠️ `.pcar` 는 한 덩어리가 아니었다 — `.pcar:hover .msplit__zoomhint` 와
  `.pcar__track:focus-visible ~ .msplit__zoomhint` 가 **orders.js 가 쓰는
  `.msplit__media--btn:hover/:focus-visible` 과 같은 선택자 목록**에 섞여 있었다. 그 두 줄만 빼고
  앞의 둘은 남겼다. 블록째 지웠으면 주문내역 배송사진의 '크게 보기' 힌트가 같이 죽는다.
- 실측 확인: `#/app/orders` 확대 힌트 rest 0 → hover 1 → rest 0, 키보드 포커스에서도 1.
  상품안내 자체 힌트(`.prod-media__zoom`)와 라이트박스도 그대로. 콘솔 오류 0.
- 같은 정리에서 **내가 낸 흠 하나도 고쳤다** — `onRowOpen` 을 ui.js 로 옮길 때 본문만 지우고
  JSDoc 을 두고 와서, 그 문서가 바로 아래 `editBtn` 에 붙어 있었다.
- 남겨 둔 것: `tableGrid` 의 **`headerLabel` 옵션**은 이제 호출부가 0 이다(옛 products 의 '저장'
  머리글이 유일했다). 공용 컴포넌트 API 라 이번 범위에서 빼지 않았다. `js/ui.js:5` 의 `raw` import 도
  미사용인데 **이번 변경 이전부터** 그랬다(내가 만든 것이 아니다). `components.css` 의
  `/* hm-field 안의 액션 버튼… */` 주석도 설명 대상 규칙이 없는 고아인데 역시 이전부터다.

### 남은 것
- 경조화환 샘플 사진 URL(시안·현행이 같이 쓰던 Unsplash 링크)이 지금은 **엉뚱한 사진**으로 해석된다.
  모달 뒤에 있을 땐 안 보였는데 400px 히어로가 되면서 눈에 띈다 — 교체가 필요하다.

## 이번 구간(2026-09-16 저녁) — 전체 모달 리모델링 시안 이식

claude.ai/design 프로젝트 `20160001-…05bc1` 의 **`전체 모달 리모델링.dc.html`** 을 이식했다.
시안이 세는 **12개 중 10개**를 한 규격으로 옮겼다(나머지 둘은 아래 '안 한 것').
- 새 공용 셸 **`js/util/dialog.js`**(`openDialog`·`dlgRule`·`dlgRow`·`dlgPick`·`dlgList`·`dlgActions`·`bindPick`)
  + **components.css `.dlg-*`** 블록. 옛 이름 `.odlg-*`·`.cx-*` 와 패널 클래스 셋(`--ordconfirm`·
  `--ordcancel`·`--ctc`)은 이 규격으로 합쳐 사라졌다.
- 규격: 18px 라운드 · 에어브로 + 22px 타이틀 + 34px 원형 ✕ · 1.5px 섹션 룰 · 무테 필드 ·
  **힌트 푸터**(못 누르는 이유를 그 자리에서 말한다) + 38px 필 버튼. 폭은 `--dlg-w` 로 심는다.
- 이식 대상: 담당자 지정(440) · 주문취소(480) · 주문서 삭제(440) · 자동작성(540) · 발송 프로필(500) ·
  담당자 등록/수정(560) · 담당자 삭제(440) · 거래처 담당자 관리(960) · 배송일시 피커(376).
- **자동작성이 3소스로 늘었다** — 부고장/청첩장 링크 + **텍스트 인식**. 텍스트 인식은 링크 찾기가
  아니라 **거래처가 보낸 평문 주문서를 읽는 파서**다(`js/data/order-text.js`): 배송지·받는분·연락처·
  일시·리본문구·보내는분·금액을 문장에서 찾고, 못 읽은 줄은 요청사항에 남긴다.
  ⚠️ 첫 판은 '모양으로 대충 읽기'였다가 적대적 실측에서 93케이스 중 81개가 **틀린 값**을 냈다
  (계좌번호→연락처, 전화번호→2027-02-30, '저녁 7시'→07:00, '17만 5000원'→5,000원). 제1원칙을
  '틀린 값보다 빈 칸'으로 바꿔 다시 짰고, 그때 잡은 함정을 `tools/test-order-text.mjs` (**39케이스**)에
  박아 뒀다 — **파서를 고치면 이것부터 돌릴 것**. 지금 39/39 통과(틀린 값 0건).
  ⚠️ **이 규칙 파서는 데모 기준선이다** — AI 연동 때 교체한다(아래 '보류 / 열린 항목').
- **담당자 관리 표**: 연락처가 비면 알림톡 토글을 켤 수 없다(보낼 곳이 없다).
- **배송일시 피커에서 오래된 결함 하나를 고쳤다** — 날짜를 고르면 격자가 다시 그려지며 눌린 버튼이
  DOM 에서 떨어져 `onDoc` 이 '바깥 클릭'으로 오인해 **피커가 스스로 닫혔다**(시·분을 이어서 못 골랐다).
  이제 닫히지 않고, 푸터 '취소' 가 연 시점 값으로 되돌린다.
- 검증(Playwright · 1280x720 / 1366x768): 다이얼로그 9종 실제 조작 확인 — 폭·에어브로·섹션 룰·
  힌트 전환·체크 게이트·세그먼트 전환·kind 불일치 가드·토글 잠금·피커 배치(완료 버튼 가시)·취소 복구.
  콘솔 오류 0. 검증에 쓴 store 편집은 되돌렸다.

### 안 한 것 (의도)
- **시안 #05 '주문 담당자 선택' · #06 '주문 요청자 선택' 두 다이얼로그는 만들지 않았다.**
  둘 다 **주문서 등록 모달 1단계의 인라인 카드**(`.ordnew-mgr` 칩 · `.ordnew-req` 행)로 이미
  옮겨져 있다 — 2026-09-16 사용자 지시이고(담당자 0명인 이관 거래처 18/19 때문에 다이얼로그가
  4클릭 우회로였다) 그 지시가 시안보다 뒤다. **리스타일할 다이얼로그가 존재하지 않는다.**
  되살리려면 그 지시부터 뒤집어야 한다.
- **#11 배송일시 피커는 헤더(에어브로+타이틀+✕)만 뺐다** — 모달이 아니라 앵커된 팝오버라 66px 를
  더 키우면 짧은 화면에서 `place()` 가 자리를 못 낸다(패널이 이미 466px). 폭 376·섹션 룰·흰 푸터·
  취소/완료는 적용했고, 닫는 길은 취소·완료·바깥 클릭·ESC 넷이다.
- **주 버튼 색은 시안의 파랑(#0066cc)이 아니라 브랜드 주황**이다 — 같은 출처의 '주문관리 모달
  리모델링' 을 이식할 때 이미 정한 매핑을 따랐다(파랑은 토글·선택행·정산담당 pill 같은 상태 표시 몫).

## 이번 구간(2026-09-16 오후) — 정산 일관성 3건 + 표 행 액션 리네임

화면이 사실과 다르게 말하던 자리를 고쳤다. 셋 다 정산 신뢰도에 직결된다.
- **포털 '회사정보 수정'이 저장되지 않던 것** — `state.company` 페이지 로컬에만 쓰고 끝나 화면을
  나가면 사라졌고 관리자 거래처 화면은 옛 값을 계속 봤다. 이제 `store.updateClient()` 로
  write-through 하고(포털↔관리자 **같은 레코드**), 회사명을 고치면 셸 배지도 같이 칠한다
  (`refreshClientBadge` 를 shell.js 에서 새로 export). 연락처는 `onPhoneInput` 공용 규칙.
- **계산서·명세서 상태가 발행일과 무관하던 것** — `settlementsFor` 가 월 오프셋으로 찍어
  발급일을 28일로 바꾸면 **발행일이 미래인 달이 '발급완료'** 였다. `발행일 ≤ NOW` 파생으로 바꿨다
  (시드가 전부 1일이라 그동안 가려져 있었다 — 실측으로 태원과학만 뒤집히는 것을 확인).
- **정산 드릴다운 합계 불일치** — 금액은 `usageFor`, 펼친 목록은 `b2b-mock` 주문이라 합이 안 맞고
  두 달 이전 귀속월은 목록이 통째로 비었다(근거 없는 청구서). 이제 같은 소스의 **품목별 집계 +
  합계 행**이라 구조적으로 어긋날 수 없다. 실제 주문은 주문관리로 보낸다.
- **`.ptbl-*` → `.tbl-*` 리네임 완료**(components.css 17규칙 + 거래처 목록·담당자 다이얼로그·포털
  담당자). 덤으로 코드 주석에 남아 있던 죽은 경로 `#/admin/contacts` 3곳을 담당자 관리
  다이얼로그로 바로잡았다.
- 명세서(`docs/backend-spec.md`) 3.4·6.3·6.6 을 같은 규칙으로 갱신했다.

## 직전 구간(2026-09-15 ~ 09-16) — 주문·거래처 화면 전면 개편

구 시스템 이관(그 앞 세션) 위에서, claude.ai/design 시안 3종을 이관하며 주문·거래처 화면을
다시 지었다. 커밋 56개. 아래는 **다음 세션이 알아야 할 것**만 추린 것이다.

### 주문 상세 모달 v2 — B2C·B2B 공용
`js/util/order-screen.js` 로 셸을 추출하고 두 화면이 **필드 서술자 배열만 다르게** 주입한다.
**모드 없는 상시 편집**(읽기↔편집 토글 제거 — 되살리지 말 것). 헤더 3단 스테퍼 · 좌측 350px
다크 처리 레일 · 우측 요약/처리 이력 레일 · 푸터 `수정한 항목 N개`.
- 처리 이력을 레코드에 신설(`js/data/order-history.js`) — 시간순, 새 이력은 끝에 붙는다.
- 날짜+시각 통합 피커 `makeDateTimePicker` 신설. 모달 안 팝오버는 **`position: fixed`** 여야
  조상 overflow 를 탈출한다(조상에 `transform` 이 생기면 다시 갇힌다 — 등장 애니메이션이
  transform 없는 `hmPopOrd` 인 이유).
- 거래 조건(`clientNote`)은 요약 레일 맨 아래 카드. 목록 `!` 배지는 중복이라 제거(되살리지 말 것).

### 주문 목록 — 색이 곧 순서
행 배경색은 `js/util/date.js` 의 `orderRowTone(status, 배송요청일)` **단일 소스**이고
세 화면이 공유한다(`#/admin/b2c`·`#/admin/orders`·`#/app/orders`). **정렬도 같은 색 순서**라
색과 줄 순서가 갈라질 수 없다. 행 아무 데나 누르면 상세가 열린다(안쪽 컨트롤·드래그 선택은 비켜 감).

### 거래처 정보관리
- 거래처 모달을 주문 모달과 **같은 언어**로 재작성(1400px · 다크 레일 · 상시 편집).
- 담당자를 **거래처별**로 전환(`store.contactsByClient`) — 포털 저장공간과 같은 레코드.
- 시안 이관으로 원장이 **2열 2행**이 됐다: `[거래 조건 | 회사 정보]` / `[계산서·정산 | 증빙·영업]`.
- 담당자 표는 모달에서 걷어내고 **다이얼로그**(`js/util/contacts-modal.js`)로 옮겼다.
  진입점 둘 — 목록 '관리' 열의 **사람 아이콘**(등록 인원수 배지)과 모달 레일의
  '정산·회계 담당자' 카드. 별도 탭은 만들지 않는다(2026-09-16 사용자 지시).
- 사업자등록증 첨부에 '파일 선택/변경' 추가 — 그전엔 미리보기뿐이라 관리자가 증빙을
  **새로 붙일 방법이 없었다**.

### 기업별 상품단가 v2 (`#/admin/pricing`)
좌측 거래처 pane(검색) + 분류 아코디언 + **저장 전 변경 패널**(무엇이 얼마에서 얼마로).
적용 단가는 `store.appliedPrice` **단일 소스** — 그전엔 세 화면이 규칙을 각자 복제해
같은 거래처가 상품안내에서는 계약가를, 결제 화면에서는 정가를 봤다.

### 주문서 등록 모달 (신규) — 상세와 **다른 모달**
`js/util/order-create.js` 셸(`.modal-panel--ordnew` 1240px). 상세의 `activeModal`/`editing`
을 건드리지 않는다 — 같은 변수를 쓰면 두 모달이 서로를 닫는다.
- B2C: 1단계 주문경로·결제 → 2단계 주문서. B2B: 1단계 거래처 → 2단계 상품·단가·발주정보.
- **채번은 저장 시점에만** — 그전엔 모달을 열 때 돌아 취소해도 번호가 소모됐다.
- 첫 이력은 `created`. 신규는 **담당자 미지정**으로 시작한다.
- 요청자는 **직접 입력**이 가능해야 한다 — 이관 거래처 19곳 중 **18곳이 담당자 0명**이라
  목록에서만 고르게 하면 거의 모든 거래처에서 등록이 막힌다.
- 마지막에 요청자·담당자를 **인라인 카드 2장**으로 바꿨다(다이얼로그 경로 제거, 병렬 세션 작업).
  담당자 0명이면 이름·연락처 칸이 처음부터 열려 있다.

### 고친 결함 중 기억할 것
- **스택 다이얼로그 핸들을 페이지 단일 슬롯에 담으면 안 된다** — 거부 다이얼로그가 거래처
  모달의 핸들을 덮어써 X·취소가 무반응이 됐다.
- **ESC 이중 확인은 `openModal({ onEsc })` 훅으로** — 호출부의 capture 리스너는
  `openModal` 이 먼저 등록해 이길 수 없다(실제로 죽어 있었다).
- **셸의 입력 위임이 호출부 write-through 보다 먼저 돈다** — 필수 카운터를 그 자리에서 세면
  푸터가 한 입력 늦어 마지막 칸을 채워도 등록 버튼이 비활성으로 남는다(`queueMicrotask`).
- 삭제 확인이 거래처인데 "주문서를 삭제할까요?" 라고 묻던 것(문구 하드코딩).
- 정산기한이 한 달 밀리던 것(`monthEnd` 는 0-based 인데 `getMonth()+1` 을 넘김).
- 모달 스크롤러를 그리드가 아니라 **본문 pane** 으로 — 그리드를 스크롤하면 다크 레일이
  같이 밀려 아래에 흰 띠가 드러난다.
- 라우트가 바뀌어도 남던 스택 모달 → 라우터가 화면 전환마다 `closeAllModals()`.

## 보류 / 열린 항목

> **미결의 단일 소스는 이 절이다**(2026-09-25 정리). 위 '이번 구간' 절들의 "남은 것" 은 그 시점의 기록이다.
> 명세 10.4 표와 개수·열거를 **함께** 고칠 것 — 여기만 보고 일하는 세션이 있어 Q14 가 빠진 채 두 커밋을 지난 적이 있다.
> 지금 **코드만으로 끝낼 수 있는 미결은 없다.** 아래 A 가 풀리면 코드 작업이 생긴다.

### A. 외부 입력 대기 — 자료·검토가 와야 끝난다
1. **경조화환 샘플 사진** — `js/pages/products.js:21`(`SAMPLE.경조화환`)과 `js/pages/orders.js:13`(`DELIVERY_PHOTO.근조`)이
   같은 Unsplash 이미지를 쓰는데 **엉뚱한 사진**으로 해석된다(상품 규격 안내 400px 히어로에서 눈에 띈다).
   실제 상품 사진 공급 대기 — 2026-09-25 사용자: 미결로 둔다. 받으면 두 상수를 함께 바꾼다.
2. **법무 검토** — 질문서 4문항이 명세 3.4 에 있다(2026-09-25, 보낼 수 있는 형태). 답이 오면 조항·규칙을 다시 본다.
   문구가 바뀌면 `TERMS_VERSION` 을 올려 **재동의**를 받는다(게이트가 버전을 비교).
   - ① 기한·고지 충분성 — 발급일 선택지를 1~7·11~25일로 줄여 최소 75시간은 확보했다(2026-09-25).
   - ② **정산·회계 담당자가 없는 거래처**(시드 19곳 중 18곳)는 알림 없이 자동 동의가 진행된다(대표 연락처 폴백 없음 —
     사용자 결정). 관리자 정산 표의 '알림 수신자 없음' 경고가 유일한 신호다. 막히면 폴백 발송 또는 자동 동의 보류.
   - ③ **약관에 동의하기 전의 자동 동의**(명세 10.4 Q16) — `store.agreementOf` 가 약관 동의 여부를 보지 않아, 로그인한 적 없는
     이관 거래처도 자동 동의로 계산된다. 막히면 `agreementState` 에 약관 동의 시각을 넣어 그 전 달은 자동 동의에서
     빼야 한다(테스트·관리자 표·포털 카드가 같이 바뀐다).
   - ④ 약관 ⑥항 "발급된 계산서는 취소·수정할 수 없다" 가 명세 9.1(수정계산서 발행)·세법과 충돌.
3. **세무대리인 답변** — 명세 9.1 질문서 3문항. 핵심은 발급일 11~28일 거래처의 **작성일자 = 동의일**(공급시기와 다른 달)
   취급이다(2026-09-25 에 4번 — 발급기한이 주말이면 기한 특례가 적용되는가 — 를 더했다). 답이 오면 규칙을 다시
   본다 — 대안은 귀속월 말일 소급(지연발급 가산세 리스크) 또는 발급일 1~7일만 허용(그러면 `MIN_CONSENT_DAYS` 파생의
   `INVOICE_DAYS`·테스트·안내 문구·뉴트리 15일 시연이 같이 바뀐다).
4. **카카오 알림톡 템플릿** — 10종 초안이 명세 9.4 에 있다(T1~T10, 2026-09-25). 남은 것은 **카카오 검수**와
   검수 전에 정할 셋(명세 10.4 Q17·Q15): **고객센터 번호**(`#{고객센터}` — 정해진 번호가 없다) · **'첫 주문 1회 무료'**(아래 6) ·
   **대체 발송**(알림톡 실패 시 문자로 보낼지).
5. **바로빌** — 방식은 결정(세금계산서 API + `TaxType=면세` · 세액 0 · 인증서는 운영이 바로빌 웹에서 등록).
   남은 것은 **발급 메서드명·필수 필드**(착수 시 개발자센터 레퍼런스에서 확정)와 **발급 대행 계약 조건(요금·SLA)**(명세 9.1).
   ⚠️ 클라우드 세션에서는 `dev.barobill.co.kr` 이 네트워크 정책에 막혀 레퍼런스를 읽을 수 없었다(2026-09-25).
6. **'신규 가입 1회 무료'**(명세 10.4 Q15) — 가입 화면(`register.js` 혜택 목록·완료 화면)이 약속하는데 정산 규칙·명세에 없다.
   어느 주문이 0원이 되는지, 명세서·계산서에 어떻게 찍히는지 사용자 결정이 필요하다. 알림톡 T9 가 이 문장을 싣는다.

### B. 결정됐고 "그때" 하기로 한 것
- **Q5 텍스트 인식 파서 → AI 연동 때 교체**(명세 10.4 Q5 · 9.3.2). 지금 `js/data/order-text.js` 는 규칙 기반 데모다.
  갈아끼우는 것은 `parseOrderText` 내부뿐이고, `tools/test-order-text.mjs` 39케이스를 **그대로 판정 기준**으로 쓴다
  (채운 비율이 아니라 틀린 값 건수). 정확도 개선 작업은 이 결정으로 닫혔다 — 손대기 전에 AI 연동 일정부터 확인.
- **Q8 모바일 → 별도 모바일 앱 연동**(2026-09-17 재정의). 웹 반응형은 하지 않고 포털·관리자 웹은 데스크톱 전용.
  범위·API·일정은 사용자가 다시 작업하기로 했다. (구 harim 의 `mobile-order/` 미이식도 같은 마일스톤이다.)
- **포털↔관리자 주문 연동 → 서버 붙일 때**(2026-09-17 재확인, 명세 6.4). `#/app/orders` 와 `#/admin/orders` 는 각자
  목데이터다. 그 결과 실데이터(`invoice-mock.js`)가 **없는** 달은 관리자만 난수로 청구하고 포털 명세서는 '내역 없음' 이다.

### C. 서버 전환 시 구현할 것 — 명세에 확정됐고 데모는 흉내만 낸다
| 항목 | 명세 | 데모는 지금 |
|---|---|---|
| 자동 동의를 **기록**하는 잡(마감 13:00 → `AGREED(auto)` → 바로빌 발급) | 3.4 · 5.4 | 기록 없이 매번 파생 |
| 발급일 이력 테이블(`client_id, effective_period, invoice_day`) · 과거 Settlement 재계산 금지 | 3.4 · 4.2 | `client.invoiceDayLog` |
| 동의 API 는 마감 전에만(`409`), 링크 발급 API 는 발행 전 거부(`409`) | 6.6 | 화면 버튼 잠금 |
| 감사 로그 기록(조회 화면 없음) · 거래처 모달 '변경 이력' 카드를 감사 로그로 채움 | 8.4 | 세션 한정 배열 |
| 공개 링크: 서버 토큰(128bit) · 60일 만료 · 재발급 시 구 토큰 무효 · 열람 로그(DB) · noindex · 레이트 리밋 | 8.3 | 만료·로그 없음 |
| 주문일시 `ordered_at` 한 컬럼(ISO 8601 `+09:00`) · 프런트 `receivedAt`/`date` 는 어댑터 매핑 | 5.3 | 두 키·두 포맷 |
| 약관 동의 기록(`termsAgreedAt`·`termsVersion`) 저장 + 버전 비교 게이트 | 3.1 · 4.2 | localStorage |
| 알림톡 발송 10종(T1~T10 — 시점·수신자·버튼은 명세 표) · 수신자 없으면 미발송 | 9.4 | 발송 없음 |
| 배송완료 리포트 링크를 **토큰 URL** 로(공개 명세서 링크와 같은 capability 방식) | 9.4 · 8.3 | `delivery/?order=…&recipient=…` 쿼리스트링에 개인정보 |
| 인증·역할·상태 검증 — `js/session.js` 는 DEMO 게이트다(관리자 비밀번호가 클라이언트 JS 에 있다). 활성 거래처만 로그인(`403` 상태별 코드), 정지·반려·삭제 시 세션 무효화 | 6.2 · 7 · 8.1~8.2 | sessionStorage + 화면 이동마다 재확인 |

### D. 데모·검증 때 알아야 할 것 (결함 아님)
- **거래처 계정 첫 로그인 = 약관 동의 게이트**(이관 19곳 전부 동의 기록이 없다). Playwright 는 `[data-action='ack']` 체크 →
  `[data-action='ok']`. 관리자(admin)는 게이트가 없다.
- **포털 라우트는 로그인 필수** — 비로그인 딥링크는 `#/login` 으로 갔다가 로그인 후 원래 주소로 돌아온다.
- **거래처 로그인은 아이디로만** — 아래 '데모 계정' 의 아이디가 아니면 들어가지 못한다(사업자번호도 거절). 승인대기·반려·정지
  거래처는 로그인 화면에서 막힌다 — 승인 흐름 시연은 가입 → 로그인 시도(막힘) → 관리자 승인 → 로그인.
- **(주)뉴트리 발급일 15일**(11~28 그룹 시연)은 localStorage 가 비어 있어야 보인다(`hydrate` 가 저장값 우선).
- **발급일 드롭다운은 22개**(1~7·11~25일) — 8~10·26~28일은 고를 수 없다. 예전에 저장된 그런 값은 트리거에
  '지금은 고를 수 없는 날' 로 보이고 계산은 그 값 그대로다.
- **발송 프로필 시드 5건은 태원과학 소속** — 다른 거래처로 로그인하면 프로필 0건이 정상(직접 등록).
- **포털 주문 퍼널 '주문 접수하기'는 저장하지 않는다** — 관리자 주문관리에도 포털 '내 주문'에도 안 뜬다(B 참조).
- **정산 표 '알림 수신자 없음' 18곳**은 이관 데이터 특성(담당자 0명)이다 — 담당자 관리 다이얼로그로 채우면 사라진다.
- **승인대기 시드 0건** — 회원가입을 태우거나 거래처 모달 상태 pill 로 2클릭에 만든다(대쉬보드 승인 카드도 그때만).

### E. 일부러 남긴 것 — 다시 올라오면 이 근거로 넘길 것
- **관리자 대리 동의 없음**(2026-09-17 결정, 명세 3.4 예외표). 필요해지면 `agreed_mode=admin` 을 추가한다.
- **채널 수수료·정산 주기라는 개념은 없다**(Q11, 디자인 단계 오류로 제거) — 되살리지 말 것.
- **CSS 키트 변형 6개**(`.btn-primary`·`.btn-success`·`.tint-green`·`.tint-orange`·`.field-error`·`.hm-success`) — 사용처 0
  이지만 형제가 살아 있는 공용 키트의 변형이라 유지. 색 세트를 반쪽만 남기는 쪽이 더 나쁘다.
- **`js/data/intake-guide.js` 고아 모듈** — 화면에서만 내렸고 데이터는 보존(사용자 지시). 정리하지 말 것.
- **근조화환 B2C 퍼널 사이트는 이 레포 대상 아님** — 다시 요청받으면 대상 프로젝트부터 확인.
- **실거래처 개인정보 공개 유지**(2026-09-25 사용자 결정) — 저장소 public·사이트 공개 상태에서 `admin-mock.js` 시드의
  대표자명·대표 연락처·거래 조건 속 인명과 `staff-mock.js` 번호 2개를 그대로 둔다. 다시 올라오면 이 결정으로 넘기되,
  **실데이터를 새로 더 넣을 때는 다시 물을 것**(이 결정은 이미 올라간 데이터에 대한 것이다).

## 참조 위치
- auto-memory: `~/.claude/projects/C--Users-ehgns-Documents-GitHub-Yearseventfigma/memory/MEMORY.md`
- 백엔드 계약 정본: `docs/backend-spec.md` (수정 후 `python tools/build-docs.py`)
- 시안 정본(claude.ai/design · `DesignSync` 툴):
  - 주문 모달·등록 모달: 프로젝트 `20160001-e573-4e60-8ed6-01a39ae05cb1`
  - 거래처 정보 수정 모달: 프로젝트 `755f33a7-35c3-476c-875b-22d0ae956090`
  - 필터 영역: 프로젝트 `5ad2963b-4434-40a9-bd0d-df144dd4d45b`

## 데모 계정
- **관리자**: `admin` / `0324`
- **거래처**: 이관 시드에는 비밀번호가 없어 **아이디만으로 로그인**한다(비밀번호는 아무 값).
  첫 로그인에는 **이용약관 동의 게이트**가 뜬다(체크 → '동의하고 시작').
  `taewonsci`(태원과학 — 담당자 3명·계약단가 데모) · `homepack`(홈팩 — 거래 조건 데모) ·
  `sejong-kds`/`sejong-lbh`(법무법인 세종 — 같은 사업자번호 부서 분리 데모) ·
  `sodamchae` · `jinyang` · `ksanit` · `bluesea` · `ilpumchae` · `goifuneral` · `wholefresh` ·
  `knmetal` · `nutree` · `peellaw` · `daehyang` · `jobis` · `overlay` · `comverse` · `cnb`
  (**19곳 중 18곳은 담당자 0명** — 구 시스템에 대응 필드가 없어서다. 버그가 아니며,
  주문 등록·담당자 관리 화면이 이 비율을 전제로 설계돼 있다.)
