# HANDOFF — 세션 인수인계

> 마지막 갱신: 2026-09-15 · HEAD `e820e79d` · origin/main 동기화 · 작업 규약은 [CLAUDE.md](CLAUDE.md) 참조

## 현재 상태
- 배포: main 푸시 → GitHub Pages 자동(레포 루트 전체).
- 미커밋 변경 없음. 미추적 파일(`mockups/`·`백엔드 참고문서.docx`·삭제된 `guidelines/`)은 의도적 로컬 상태 — 건드리지 말 것.
- `docs/*.docx` 는 생성물이라 `.gitignore` 대상. 원본은 `docs/backend-spec.md`.
- 개발 서버는 꺼져 있을 가능성 높음 → `node serve.mjs` (localhost:8000).

## 직전 세션(2026-09-15) — 구 시스템 이관 완료

구 관리자(flowerdel.pe.kr/adm2) 11개 화면과 신규를 대조한 체크리스트 26개 항목을
처리했다. **구 시스템에 직접 접속해 실데이터를 읽어** 추정을 사실로 교체했다.

### 뒤집힌 전제 2가지 (중요)
1. **지역 규칙은 배송비 할증이 아니라 「반입 가능 상품 허용 목록」**이다. 구 25건 중
   23건이 "이 지역·이 장례식장에서는 *이 상품만* 반입 가능"이고 배송불가는 2곳뿐.
   기존 할증 13건과 지역이 거의 안 겹쳐 **대체가 아니라 병존**해야 했다.
2. **「거래처 참고사항」은 메모가 아니라 거래 조건**이다((주)홈팩: "무조건 특대상품
   발송"). 19곳 중 12곳이 갖고 있고 누락되면 주문마다 사고가 난다.

### 이관된 실데이터
- **거래처 19곳** — 구 23곳에서 꽃집 3곳(협력 화원, 범위 밖)과 테스트 레코드 1건 제외.
  `clientNote`(거래 조건) 신설. 담당자명·이메일은 **구에 대응 필드가 없어 공란**.
  법무법인 세종 2건이 같은 사업자번호 → 부서 접기 UI가 실데이터에서 동작.
- **계정 3개** — 최근 1년 로그인만(`admin`·`dev`·`mayflower`). 비밀번호는 이관하지 않음.
- **지역 규칙 38건** — 구 반입 25 + 기존 할증 13을 `type`(blocked/allowlist/surcharge)
  으로 통합(`js/data/intake-rules.js`). 신안군은 할증 → **배송불가**로 교체.
- 폐기: 주문 5,639 · 설문 174 · 계약 영업정보 25.

### 신규 화면
- **`#/admin/orders` 거래처 주문관리** — 구 202번 화면의 빈자리. B2C와 분리한 이유는
  운영 절차가 다르기 때문(귀속월·적용 단가·담당자 배정 유무). 거래 조건이 있는 거래처는
  목록에 `!`, 상세 최상단에 경고 배너.
- **`#/admin/dashboard` 대쉬보드** — 3층(처리대기 / 오늘 유입 / 이번 달).
  B2C는 주문경로별, B2B는 거래처별로 분리. **모든 수치는 목록과 같은 소스에서 파생.**
- `#/admin/staff` → **담당자 계정·권한**으로 확장(아이디·권한). 최근 로그인은 화면에 두지 않는다 — 감사 로그가 출처.

### 고친 것
- **거래처 바인딩 통일** — 셸 배지가 진양 하드코딩, 명세서가 C021 고정이라 한 계정으로
  들어가도 화면마다 회사가 달랐다. `js/util/client.js` 단일 소스로 세 화면을 묶음.
- **비밀번호 평문 노출 제거** — 거래처·담당자 모달에서 입력칸을 빼고 임시비밀번호 발급으로.
- **취소 사유·수수료** — 데이터 필드와 저장 로직은 있었는데 **입력 UI만 없어서** 값이 영원히
  비어 있었다. 코드값 6종 + 수수료(`js/util/cancel-modal.js`, B2C·B2B 공용).
- **알림 수신자 단일 명단** — 받는분·보내는분·담당자·추가 수신자를 한 목록으로. 담당자는
  저장공간에서 '수신함'인 사람이 `자동` 배지로 편입되고 이 주문에서만 끌 수 있다.
- **주소검색(우편번호)** — 로드 실패 시 버튼을 감추고 직접 입력 유지(폴백이 기본).
- **정산 청구금액 → 근거 주문 인라인 드릴다운**.
- **배송 옵션 정리** — 긴급·야간 제거. `evening`/`night` 시간대 구분도 함께 정리(`afterClose`).
- **사업자등록증 첨부 + 영업 필드** — 승인대기 행에도 상세 버튼(등록증을 못 보고 승인하던 문제).
- `b2c-mock` 날짜가 2026/07 고정이라 '오늘' 집계가 영원히 0이었다 → DATA_NOW 상대 생성.

### 문서
`docs/backend-spec.md` 10장 1권(PRD·유저플로우·기능명세·데이터모델·API·권한·보안·
외부연동·부록). `python tools/build-docs.py` → Word 생성(외부 라이브러리 없음).

## 보류 / 열린 항목
- **미결 8건은 명세서 10.4에 정리** — 부분 입금 이월, 단가 변경 이력·소급, 감사 로그 화면,
  바로빌 인증서 보관, AI 파싱 정확도 기준, 자동 동의 약관 문구, 거래처 신용 한도, 모바일.
- **포털↔관리자 주문 동작 연동 없음** — `#/app/orders`(16건)와 `#/admin/orders`(44건)는
  각자 목데이터다. 사용자가 "화면만 만들고 연결은 계약서로"로 결정. 규칙은 명세서 6.4.
- **정산 드릴다운의 합계 불일치** — 청구금액은 `usageFor`, 주문 목록은 `b2b-mock` 에서
  각각 파생돼 합이 안 맞는다. 실서비스에선 같은 주문 집합에서 나와야 한다(코드에 주석).
- 승인대기 거래처가 시드에 0건 — 회원가입 플로우를 태워야 생긴다.
- `settlement.js` 회사정보수정 모달은 아직 로컬 편집만(store write-through 없음).
- 계산서 상태를 `발행일 ≤ NOW` 에서 파생하기.
- `.ptbl-*` → `.tbl-*` 리네임.
- `#/app/*` 라우트 가드 부재 · `isAuthed()` 데드 API.
- **의도적 미이식(H→Y)**: `mobile-order/`(별도 마일스톤).
- **근조화환 B2C 퍼널 사이트는 이 레포 대상 아님** — 다시 요청받으면 대상 프로젝트부터 확인.

## 참조 위치
- auto-memory: `~/.claude/projects/C--Users-ehgns-Documents-GitHub-Yearseventfigma/memory/MEMORY.md`
- 이번 세션 계획서: `~/.claude/plans/refactored-sauteeing-elephant.md`
- 이관 체크리스트 아트팩트: https://claude.ai/code/artifact/0983fa22-f14f-49ff-9efe-84a6d88cb02f
- B2C 모달 미리보기 아트팩트: https://claude.ai/code/artifact/b89f6bff-be29-43a3-85ee-181df9dcfd47
- 필터 시안 정본: claude.ai/design 프로젝트 `5ad2963b-4434-40a9-bd0d-df144dd4d45b` (`DesignSync`)

## 데모 계정
- **관리자**: `admin` / `0324`
- **거래처**: 이관 시드에는 비밀번호가 없어 **아이디만으로 로그인**한다(비밀번호는 아무 값).
  `taewonsci`(태원과학) · `sodamchae`(소담채) · `jinyang`(진양) · `ksanit`(대한위생사협회) ·
  `sejong-kds`/`sejong-lbh`(법무법인 세종 — 부서 분리 데모) · `homepack`(홈팩 — 거래 조건 데모) ·
  그 외 `bluesea`·`ilpumchae`·`goifuneral`·`wholefresh`·`knmetal`·`nutree`·`peellaw`·
  `daehyang`·`jobis`·`overlay`·`comverse`·`cnb`
